# FreeOrPaidAndroidApp
A python script, which when given the package name, tells if the app is paid or not.<br>
Usage: Pass the package name as an argument.<br>
Ex: python FreeOrPaid.py org.synergylabs.pmpandroid
